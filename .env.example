# Postgres connection string (see prisma/schema.prisma)
DATABASE_URL="postgresql://user:password@localhost:5432/dungeon_crawl"

# Claude API key for the LLM narrator/intent-parser.
# Not wired in yet — src/lib/llm/parse-intent.ts and src/lib/llm/narrate.ts
# are placeholder implementations until this is connected.
ANTHROPIC_API_KEY=""
